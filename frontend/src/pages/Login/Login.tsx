import LoginForm from "../../features/auth/components/LoginForm";

export default function Login() {
    return (
        <main id="login-page">
            <h1>Login Page</h1>
            <LoginForm></LoginForm>
        </main>
    );
}
